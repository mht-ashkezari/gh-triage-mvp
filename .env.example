# ── Auth Option A: Personal Access Token (classic PAT) ────────────────────────
# Use this if you just want to read public repos (or your own private repos).
# Example format: ghp_************************************
# Leave blank in the example file (never commit real secrets).
GITHUB_TOKEN=

# ── Auth Option B: GitHub App installation token (least-privilege) ───────────
# If you created a GitHub App in P0.2, fill these locally (not in git):
#   GITHUB_APP_ID:             the numeric App ID
#   GITHUB_INSTALLATION_ID:    the numeric installation ID
#   GITHUB_PRIVATE_KEY_BASE64: base64 of your PEM (single line, no newlines)
GITHUB_APP_ID=
GITHUB_INSTALLATION_ID=
GITHUB_PRIVATE_KEY_BASE64=

# ── Optional quality-of-life (leave blank unless you use them) ───────────────
# If you want to prefer gh CLI token automatically:
# GH_TOKEN=


# Security / Compliance 
SECURITY_SCAN_ENABLED=true
ALLOWED_EGRESS_HOSTS=api.github.com,login.microsoftonline.com
KEYVAULT_URI=https://placeholder.vault.azure.net/
EU_REGION=swedencentral

