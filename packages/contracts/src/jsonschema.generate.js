"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const node_fs_1 = __importDefault(require("node:fs"));
const node_path_1 = __importDefault(require("node:path"));
const zod_to_json_schema_1 = require("zod-to-json-schema");
const entities = __importStar(require("@ghtriage/schemas/src/entities"));
const common = __importStar(require("@ghtriage/schemas/src/common"));
const outDir = node_path_1.default.resolve(process.cwd(), "../../docs/jsonschema");
node_fs_1.default.mkdirSync(outDir, { recursive: true });
const all = { ...entities, ...common };
for (const [name, schema] of Object.entries(all)) {
    // @ts-ignore
    const json = (0, zod_to_json_schema_1.zodToJsonSchema)(schema, name);
    node_fs_1.default.writeFileSync(node_path_1.default.join(outDir, `${name}.schema.json`), JSON.stringify(json, null, 2));
}
console.log(`JSON Schemas written to ${outDir}`);
